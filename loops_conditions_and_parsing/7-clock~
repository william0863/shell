#!/usr/bin/env bash
# This script will display the time for 12hours and 59minutes.

h=0
x=1
while [ "$x" -le 59 ]
      do echo "$x"
   if [ "$x" -eq 59 ]
   then echo "Hour: $h"
   elif [ $h -eq 12 ]
   then return 0
   else :
   fi

   (( h++ ))
   (( x++ ))
done



